# ============================================================
# Emergency Evacuation AI - Python Dependencies
# Python 3.11+
# ============================================================
# Install: pip install -r requirements.txt
# Note: Some packages are optional. The project runs in demo
# mode without GPU, SUMO, OSRM, Redis, or PostgreSQL.
# ============================================================

# -----------------------------------------------------------
# AI / Machine Learning
# -----------------------------------------------------------
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.5.0
scipy>=1.12.0

# -----------------------------------------------------------
# Computer Vision
# -----------------------------------------------------------
opencv-python>=4.10.0
ultralytics>=8.0.0
# Note: ultralytics includes YOLO model downloading.
# In demo mode, a fallback detector is used if weights
# are unavailable or download fails.

# -----------------------------------------------------------
# Backend API
# -----------------------------------------------------------
fastapi>=0.115.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
uvicorn[standard]>=0.30.0

# -----------------------------------------------------------
# Agentic AI
# -----------------------------------------------------------
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# -----------------------------------------------------------
# Routing / Graph / Optimization
# -----------------------------------------------------------
networkx>=3.0
# scipy is listed above under AI/ML

# -----------------------------------------------------------
# Database
# -----------------------------------------------------------
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
# Optional: uncomment for PostgreSQL
# psycopg[binary]>=3.0.0
# geoalchemy2>=0.15.0  # For PostGIS support

# -----------------------------------------------------------
# Configuration / Environment
# -----------------------------------------------------------
python-dotenv>=1.0.0
pyyaml>=6.0.0

# -----------------------------------------------------------
# Communication / Caching
# -----------------------------------------------------------
redis>=5.0.0
# Note: Redis is optional. The project uses an in-process
# LocalEventBus if Redis is unavailable.

# -----------------------------------------------------------
# HTTP / External Services
# -----------------------------------------------------------
requests>=2.31.0
httpx>=0.27.0

# -----------------------------------------------------------
# Testing
# -----------------------------------------------------------
pytest>=8.0.0
pytest-asyncio>=0.23.0

# -----------------------------------------------------------
# Utilities
# -----------------------------------------------------------
Pillow>=10.0.0
matplotlib>=3.8.0